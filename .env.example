# OPTIONAL. The app ships with a working public Supabase config baked in
# (see src/lib/supabaseConfig.js), so it runs without a .env at all.
#
# Set these only to point the app at a DIFFERENT Supabase project — they
# override the committed config. Vite exposes vars prefixed with VITE_.

VITE_SUPABASE_URL=https://uhyslmfvseliykfhhwyh.supabase.co

# anon / publishable key (safe to expose; protected by Row Level Security)
VITE_SUPABASE_ANON_KEY=sb_publishable_OQ52YxyA9Iw55krbelizPA_SqYeNqWh
